<?php
// Rename this file to kc_remote.php to enable remote KingsChat mode.
// This makes the admin panel call a remote KingsChat service you control.
//
// 1) Deploy the API endpoints in kingschat/api/* to your external server.
// 2) Set KC_REMOTE_BASE to that server’s base path (without trailing slash).
// 3) Set KC_REMOTE_API_KEY to the same key configured on the remote server.

// Example:
// define('KC_REMOTE_BASE', 'https://your-domain.tld/kingschat');
// define('KC_REMOTE_API_KEY', 'paste-the-same-strong-key-here');

// No closing tag to avoid accidental output

